# Ces variables se configurent dans le tableau de bord Vercel (Settings →
# Environment Variables), pas dans un fichier local — voir DEPLOY.md.

DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
JWT_SECRET="une-longue-chaine-aleatoire-a-generer"
